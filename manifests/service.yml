apiVersion: v1
kind: Service
metadata:
    name: "aksk8scluster-1447"
    labels:
        app: "aksk8scluster-1447"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aksk8scluster-1447"